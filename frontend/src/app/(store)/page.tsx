import Catalog from './Catalog';

/**
 * Storefront landing = the product catalog ("The Autumn Edit"). RTK
 * Query-driven grid with filters, sort, search, pagination, and a featured row.
 */
export default function Home() {
  return <Catalog />;
}
